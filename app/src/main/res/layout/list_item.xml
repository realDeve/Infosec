<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    app:cardCornerRadius="3dp"
    android:layout_margin="4dp"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:padding="16dp"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/title"
            android:textSize="19sp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@+id/newsimage"
            android:lines="1"
            android:textColor="@android:color/black"
            android:fontFamily="@font/default_semibold"
            android:text="NHL pulling faulty pucks with tracking tech"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/describtion"
            android:layout_marginTop="5dp"
            android:textSize="17sp"
            android:textAppearance="@style/TextAppearance.AppCompat.Small"
            android:layout_marginRight="9dp"
            app:layout_constraintTop_toBottomOf="@+id/title"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@+id/newsimage"
            android:lines="3"
            android:ellipsize="end"
            android:text="The NHL says it is temporarily pulling pucks with tracking technology because they aren't up to spec."
            android:fontFamily="@font/default_regular"/>

        <ImageView
            android:layout_width="80dp"
            android:id="@+id/newsimage"
            android:layout_height="80dp"

            android:scaleType="centerCrop"
            app:layout_constraintRight_toRightOf="parent"
            android:background="@color/liter"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
